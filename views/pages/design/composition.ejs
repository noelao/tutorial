<div class="breadcrumb">
  Ancang-ancang <span>›</span> Perkenalan
</div>

<div class="page-header">
  <div class="tag-row">
    <span class="tag tag-yellow">Work In Progress</span>
  </div>
  <h1 class="page-title">Composisi Deisgn Web</h1>
  <p class="page-desc">
    Demi memudahkan, dan menjaga pengembang untuk menentukan dan menyamakan besar ukuran setiap petakan di dalam sebuah halaman.
    pengembang harus secara sadar menentukan variable seperti tinggi, dan lebar.
  </p>
  
</div>

<div class="content">
    <h2>Mencacah Halaman</h2>
    <p>Membagi 1 lebar halaman penuh menjadi beberapa bagian.</p>
    
    <h4>Skill yang di butuhkan</h4>
    <div class="tag-row">
      <span class="tag">v2.1.0</span>
      <span class="tag tag-green">Stable</span>
      <span class="tag tag-blue">5 min read</span>
      <span class="tag tag-red">Deprecated</span>
      <span class="tag tag-yellow">Beta</span>
    </div>
    
    <div class="callout callout-info">
        <div class="callout-label">Info</div>
        Karena setiap device memiliki lebar halaman yang berbeda maka banyaknya cacahan juga berbeda.
    </div>
    
    <h4>Lebar Laptop 1440 px</h4>
    <p>ada 12 potongan dengan gap <code>12px</code></p>


<div class="codeblock">
  <div class="codeblock-header">
    <span class="codeblock-filename">style.css</span>
    <button class="copy-btn" onclick="copyCode(this, `
    .garisPotong {
        flex-grow: 1;
        width: 100%;
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        gap: 12px;
        .potong {
            width: 100%;
            height: 100%;
            background-color: var(--biru);
        }
    }
`)">copy</button>
  </div>
  <div class="codeblock-body">  <span class="fn">.garisPotong</span> {
      <span class="kw">display</span>: <span class="str">grid</span>;
      <span class="kw">grid-template-columns</span>: <span class="fn">repeat</span>(<span class="num">12</span>, <span class="str">1fr</span>);
      <span class="kw">gap</span>: <span class="num">12px</span>;
      <span class="fn">.potong</span> {
          <span class="kw">width</span>:            <span class="str">100%</span>;
          <span class="kw">height</span>:           <span class="str">100%</span>;
          <span class="kw">background-color</span>: <span class="fn">var</span>(<span class="var">--biru</span>);
      }
  }
</div>
</div>



    <div class="callout callout-warn">
        <div class="callout-label">Perhatian</div>
        Hal ini bertujuan mempermudah pengembang, jadi demi kreatifitas, pengembang memiliki kebebasan penuh terhadap webnya, jadi jangan terpaku pada aturan, tetapi aturan memang memudahkan.
    </div>


</div>


<!-- ── Script tabs ── -->
<script>
  function switchTab(containerId, tabId, btn) {
    const container = document.getElementById(containerId)
    container.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'))
    container.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'))
    btn.classList.add('active')
    container.querySelector(`[data-tab="${tabId}"]`).classList.add('active')
  }

  function copyActiveTab(containerId) {
    const container  = document.getElementById(containerId)
    const activePanel = container.querySelector('.tab-panel.active')
    const text = activePanel ? activePanel.innerText : ''
    const btn  = container.querySelector('.tab-actions .copy-btn')
    navigator.clipboard.writeText(text).then(() => {
      btn.textContent = 'copied!'
      btn.classList.add('copied')
      setTimeout(() => { btn.textContent = 'copy'; btn.classList.remove('copied') }, 2000)
    })
  }
</script>
